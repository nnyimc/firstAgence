{% extends 'base.html.twig' %}

{% block title %} Nos propriétés {% endblock %}

{% block body %}
    <div class="container">
        <div class="main mt-2">
            <h1>Nos propriétés</h1>

            <div class="row flex">
                {{ include('/pages/_proprietesCards.html.twig')}}
            </div>


            <div class="pagination">
                {{ knp_pagination_render(proprietes) }}
            </div>

        </div>
    </div>
{% endblock %}