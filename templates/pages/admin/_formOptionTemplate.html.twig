<div class="col-md-6">
    {{ form_start(formOption) }}

    <div class="row">
        <div class="col-md-6">{{ form_row(formOption.intitule)}} </div>
    </div>

    {{ form_rest(formOption) }}
    <button class="btn btn-primary">{{ button | default('Enregistrer')}}</button>
    {{ form_end(formOption) }}
</div>
